requests==2.32.3
pandas==2.2.2
psycopg2-binary==2.9.9
SQLAlchemy==2.0.32
pytest==8.3.2
python-dotenv==1.0.1
boto3==1.34.162          # S3 / MinIO client for the bronze landing zone
# dbt-postgres==1.8.6    # uncomment to run dbt locally (kept separate —
                          # heavy dependency, not needed for Airflow container)
